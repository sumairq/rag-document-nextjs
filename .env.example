# --- Database ---
# Local dev Postgres (see docker-compose.yml). pgvector image, db "ragdb".
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/ragdb

# --- AI provider ---
# Which provider implementation to use. Currently: "gemini".
AI_PROVIDER=gemini

# Google Gemini API key. Get one at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Model names (overridable so you can bump versions without code changes).
EMBEDDING_MODEL=gemini-embedding-001
GENERATION_MODEL=gemini-2.0-flash
